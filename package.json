{
  "name": "sel",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@8.12.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "clean": "rm -rf {packages,apps}/*/{dist,tsconfig.tsbuildinfo}"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.7.2"
  }
}
